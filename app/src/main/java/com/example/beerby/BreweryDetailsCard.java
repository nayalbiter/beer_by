//Carmen Albiter
package com.example.beerby;

import android.os.Bundle;
import android.view.View;

import androidx.annotation.Nullable;
import androidx.appcompat.app.AppCompatActivity;

public class BreweryDetailsCard extends AppCompatActivity {
    @Override
    protected void onCreate(@Nullable Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
    }

    //this class creates an object of BreweryInfo that displays all the information of the brewery

    //button that returns to the list results from the detailed brewery card
    public void comeBackToTheList(View view) {

        //creates an intent that goes back to the list of breweries
    }
}
